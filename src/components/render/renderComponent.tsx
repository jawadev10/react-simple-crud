import {EventCreationBuilder} from "../../models/EventCreationBuilder";


const keysToComponentMap = {
    //table: Table,
    // text
    //select
    //range_picker
    //textarea
}

export const renderComponent = (config: EventCreationBuilder) => {
   /** if(typeof keysToComponentMap[config.component] !== "undefined") {

    } **/
}